# Go Web Server

This repo can be used as a starting point to deploy [Go](https://golang.org/) web applications on TroqCloud.

It's a simple `net/http` Go webserver that embeds an HTML file and serves it.
